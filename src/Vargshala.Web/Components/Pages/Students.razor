@page "/students"
@using Vargshala.Contracts.Students
@rendermode InteractiveServer

<PageTitle>Students Directory | Vargshala</PageTitle>

<div class="space-y-6">
    
    <!-- Top Header & Action Controls -->
    <div class="flex flex-col md:flex-row md:items-center justify-between gap-4">
        <div>
            <h1 class="text-xl font-extrabold text-slate-800 tracking-tight">
                Students Directory
            </h1>
            <p class="text-xs text-slate-500 mt-0.5">
                Manage enrolled students, batch assignments, contact details, and fee records.
            </p>
        </div>

        <div class="flex items-center gap-3">
            <button type="button" 
                    @onclick="ExportCsv"
                    class="px-3.5 py-2 rounded-xl text-xs font-semibold bg-white border border-tealborder text-slate-700 hover:bg-slate-50 transition-all flex items-center gap-2 shadow-xs">
                <svg class="w-3.5 h-3.5 text-slate-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                </svg>
                <span>Export CSV</span>
            </button>

            <button type="button" 
                    @onclick="OpenAddModal"
                    class="btn-brand text-xs px-4 py-2 flex items-center gap-2 shadow-md">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4"/>
                </svg>
                <span>Add Student</span>
            </button>
        </div>
    </div>

    <!-- Quick Stats Cards Summary -->
    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
        <div class="card-surface p-4 flex items-center gap-3.5">
            <div class="w-10 h-10 rounded-xl bg-teal-50 text-brand-700 flex items-center justify-center font-bold text-sm shrink-0">
                <svg class="w-5 h-5 text-[#009488]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>
                </svg>
            </div>
            <div>
                <p class="text-[11px] font-medium text-slate-500">Total Students</p>
                <h3 class="text-lg font-extrabold text-slate-800">1,248</h3>
            </div>
        </div>

        <div class="card-surface p-4 flex items-center gap-3.5">
            <div class="w-10 h-10 rounded-xl bg-emerald-50 text-emerald-600 flex items-center justify-center font-bold text-sm shrink-0">
                <svg class="w-5 h-5 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
            </div>
            <div>
                <p class="text-[11px] font-medium text-slate-500">Active Students</p>
                <h3 class="text-lg font-extrabold text-emerald-600">1,180</h3>
            </div>
        </div>

        <div class="card-surface p-4 flex items-center gap-3.5">
            <div class="w-10 h-10 rounded-xl bg-rose-50 text-rose-600 flex items-center justify-center font-bold text-sm shrink-0">
                <svg class="w-5 h-5 text-rose-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
            </div>
            <div>
                <p class="text-[11px] font-medium text-slate-500">Fee Overdue</p>
                <h3 class="text-lg font-extrabold text-rose-600">68</h3>
            </div>
        </div>

        <div class="card-surface p-4 flex items-center gap-3.5">
            <div class="w-10 h-10 rounded-xl bg-amber-50 text-amber-600 flex items-center justify-center font-bold text-sm shrink-0">
                <svg class="w-5 h-5 text-amber-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/>
                </svg>
            </div>
            <div>
                <p class="text-[11px] font-medium text-slate-500">New This Month</p>
                <h3 class="text-lg font-extrabold text-amber-600">+42</h3>
            </div>
        </div>
    </div>

    <!-- Search & Filter Controls -->
    <div class="card-surface p-4 flex flex-col md:flex-row items-stretch md:items-center justify-between gap-3">
        <!-- Search Input -->
        <div class="relative flex-1 max-w-md">
            <span class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none text-slate-400">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                </svg>
            </span>
            <input type="text" 
                   @bind="_searchQuery" 
                   @bind:event="oninput"
                   placeholder="Search student by name, roll no, phone..." 
                   class="w-full pl-9 pr-3 py-2 text-xs bg-slate-50 border border-slate-200 rounded-xl focus:bg-white focus:border-[#009488] focus:ring-2 focus:ring-teal-500/20 focus:outline-none transition-all placeholder-slate-400" />
            @if (!string.IsNullOrEmpty(_searchQuery))
            {
                <button type="button" @onclick="() => _searchQuery = string.Empty" class="absolute inset-y-0 right-0 flex items-center pr-3 text-slate-400 hover:text-slate-600">
                    <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
                </button>
            }
        </div>

        <!-- Filter Dropdowns -->
        <div class="flex flex-wrap items-center gap-2">
            <!-- Batch Filter -->
            <select @bind="_selectedBatch" 
                    class="text-xs bg-slate-50 border border-slate-200 rounded-xl px-3 py-2 text-slate-700 focus:bg-white focus:border-[#009488] focus:outline-none cursor-pointer">
                <option value="">All Batches</option>
                <option value="Class 10 — Maths">Class 10 — Maths</option>
                <option value="Class 10 — Science">Class 10 — Science</option>
                <option value="Class 12 — Physics">Class 12 — Physics</option>
                <option value="Class 12 — Chemistry">Class 12 — Chemistry</option>
                <option value="IIT-JEE Morning Batch">IIT-JEE Morning Batch</option>
            </select>

            <!-- Fee Status Filter -->
            <select @bind="_selectedFeeStatus" 
                    class="text-xs bg-slate-50 border border-slate-200 rounded-xl px-3 py-2 text-slate-700 focus:bg-white focus:border-[#009488] focus:outline-none cursor-pointer">
                <option value="">All Fee Status</option>
                <option value="Paid">Paid</option>
                <option value="Partial">Partial</option>
                <option value="Overdue">Overdue</option>
            </select>

            <!-- Status Filter -->
            <select @bind="_selectedStatus" 
                    class="text-xs bg-slate-50 border border-slate-200 rounded-xl px-3 py-2 text-slate-700 focus:bg-white focus:border-[#009488] focus:outline-none cursor-pointer">
                <option value="">All Status</option>
                <option value="Active">Active</option>
                <option value="Inactive">Inactive</option>
            </select>
        </div>
    </div>

    <!-- Students Data Table (Exact Pixel-Matched Layout from Screenshot) -->
    <div class="card-surface overflow-hidden border border-tealborder shadow-xs">
        <div class="overflow-x-auto">
            <table class="w-full text-left border-collapse">
                <thead>
                    <tr class="border-b border-slate-100 text-[11px] font-bold text-slate-400 tracking-wider select-none">
                        <th class="py-3.5 px-6 cursor-pointer hover:text-slate-700 transition-colors" @onclick='() => SortBy("Name")'>
                            <div class="flex items-center gap-1.5">
                                <span>Student</span>
                                @if (_sortColumn == "Name")
                                {
                                    <span class="text-[#009488]">@(_isAscending ? "▲" : "▼")</span>
                                }
                                else
                                {
                                    <span class="text-slate-300 group-hover:text-slate-400 text-[10px]">↕</span>
                                }
                            </div>
                        </th>
                        <th class="py-3.5 px-4 cursor-pointer hover:text-slate-700 transition-colors" @onclick='() => SortBy("Roll")'>
                            <div class="flex items-center gap-1.5">
                                <span>Roll</span>
                                @if (_sortColumn == "Roll")
                                {
                                    <span class="text-[#009488]">@(_isAscending ? "▲" : "▼")</span>
                                }
                                else
                                {
                                    <span class="text-slate-300 text-[10px]">↕</span>
                                }
                            </div>
                        </th>
                        <th class="py-3.5 px-4 cursor-pointer hover:text-slate-700 transition-colors" @onclick='() => SortBy("Batch")'>
                            <div class="flex items-center gap-1.5">
                                <span>Batch</span>
                                @if (_sortColumn == "Batch")
                                {
                                    <span class="text-[#009488]">@(_isAscending ? "▲" : "▼")</span>
                                }
                                else
                                {
                                    <span class="text-slate-300 text-[10px]">↕</span>
                                }
                            </div>
                        </th>
                        <th class="py-3.5 px-4 cursor-pointer hover:text-slate-700 transition-colors" @onclick='() => SortBy("Contact")'>
                            <div class="flex items-center gap-1.5">
                                <span>Contact</span>
                                @if (_sortColumn == "Contact")
                                {
                                    <span class="text-[#009488]">@(_isAscending ? "▲" : "▼")</span>
                                }
                                else
                                {
                                    <span class="text-slate-300 text-[10px]">↕</span>
                                }
                            </div>
                        </th>
                        <th class="py-3.5 px-4 cursor-pointer hover:text-slate-700 transition-colors" @onclick='() => SortBy("Status")'>
                            <div class="flex items-center gap-1.5">
                                <span>Status</span>
                                @if (_sortColumn == "Status")
                                {
                                    <span class="text-[#009488]">@(_isAscending ? "▲" : "▼")</span>
                                }
                                else
                                {
                                    <span class="text-slate-300 text-[10px]">↕</span>
                                }
                            </div>
                        </th>
                        <th class="py-3.5 px-4 cursor-pointer hover:text-slate-700 transition-colors" @onclick='() => SortBy("Fees")'>
                            <div class="flex items-center gap-1.5">
                                <span>Fees</span>
                                @if (_sortColumn == "Fees")
                                {
                                    <span class="text-[#009488]">@(_isAscending ? "▲" : "▼")</span>
                                }
                                else
                                {
                                    <span class="text-slate-300 text-[10px]">↕</span>
                                }
                            </div>
                        </th>
                        <th class="py-3.5 px-6 text-right">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-100 text-xs">
                    @if (FilteredStudents.Any())
                    {
                        @foreach (var student in FilteredStudents)
                        {
                            <tr class="hover:bg-teal-50/40 transition-colors group">
                                
                                <!-- Student: Initials Avatar + Full Name + ID -->
                                <td class="py-3.5 px-6">
                                    <div class="flex items-center gap-3">
                                        <div class="w-9 h-9 rounded-full bg-teal-50 border border-teal-200/80 text-[#00796b] font-bold text-xs flex items-center justify-center shrink-0 shadow-xs">
                                            @student.Initials
                                        </div>
                                        <div class="min-w-0">
                                            <p class="font-bold text-slate-900 leading-snug truncate cursor-pointer hover:text-[#009488]"
                                               @onclick="() => OpenViewProfile(student)">
                                                @student.Name
                                            </p>
                                            <p class="text-[10px] text-slate-400 font-medium">
                                                @student.EnrollmentNumber
                                            </p>
                                        </div>
                                    </div>
                                </td>

                                <!-- Roll Number -->
                                <td class="py-3.5 px-4 font-semibold text-slate-700">
                                    @student.RollNumber
                                </td>

                                <!-- Batch Name Pill -->
                                <td class="py-3.5 px-4">
                                    <span class="inline-flex items-center px-3 py-1 rounded-full text-[11px] font-medium bg-slate-50 border border-slate-200 text-slate-700">
                                        @student.BatchName
                                    </span>
                                </td>

                                <!-- Contact & Parent -->
                                <td class="py-3.5 px-4">
                                    <p class="font-semibold text-slate-800">
                                        @student.Phone
                                    </p>
                                    <p class="text-[10px] text-slate-400">
                                        @student.ParentName
                                    </p>
                                </td>

                                <!-- Status Badge -->
                                <td class="py-3.5 px-4">
                                    @if (student.Status == "Active")
                                    {
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-[11px] font-semibold bg-emerald-50 text-emerald-700 border border-emerald-200/80">
                                            Active
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-[11px] font-semibold bg-slate-100 text-slate-600 border border-slate-200">
                                            Inactive
                                        </span>
                                    }
                                </td>

                                <!-- Fee Status Badge -->
                                <td class="py-3.5 px-4">
                                    @if (student.FeeStatus == "Paid")
                                    {
                                        <span class="inline-flex items-center px-3 py-0.5 rounded-full text-[11px] font-semibold bg-emerald-50 text-emerald-700 border border-emerald-200/80">
                                            Paid
                                        </span>
                                    }
                                    else if (student.FeeStatus == "Partial")
                                    {
                                        <span class="inline-flex items-center px-3 py-0.5 rounded-full text-[11px] font-semibold bg-amber-50 text-amber-800 border border-amber-200/80">
                                            Partial
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="inline-flex items-center px-3 py-0.5 rounded-full text-[11px] font-semibold bg-rose-50 text-rose-700 border border-rose-200/80">
                                            Overdue
                                        </span>
                                    }
                                </td>

                                <!-- Action Menu (••• Dropdown with View profile, Edit, Collect fee, Deactivate) -->
                                <td class="py-3.5 px-6 text-right relative">
                                    <div class="inline-block text-left">
                                        <button type="button" 
                                                @onclick="() => ToggleMenu(student.Id)" 
                                                class="p-1.5 rounded-lg text-slate-400 hover:text-slate-700 hover:bg-slate-100 transition-colors">
                                            <svg class="w-4 h-4 font-extrabold" fill="currentColor" viewBox="0 0 24 24">
                                                <path d="M6 10a2 2 0 110 4 2 2 0 010-4zm6 0a2 2 0 110 4 2 2 0 010-4zm6 0a2 2 0 110 4 2 2 0 010-4z"/>
                                            </svg>
                                        </button>

                                        @if (_activeMenuStudentId == student.Id)
                                        {
                                            <div class="fixed inset-0 z-20" @onclick="() => _activeMenuStudentId = null"></div>
                                            <div class="absolute right-6 top-8 mt-1 w-36 rounded-2xl bg-white shadow-xl border border-slate-100 py-1.5 z-30 text-left animate-in fade-in zoom-in-95 duration-100">
                                                <button type="button" 
                                                        @onclick="() => OpenViewProfile(student)"
                                                        class="w-full px-3.5 py-1.5 text-xs text-slate-700 hover:bg-slate-50 flex items-center gap-2 transition-colors">
                                                    <span>View profile</span>
                                                </button>
                                                <button type="button" 
                                                        @onclick="() => OpenEditModal(student)"
                                                        class="w-full px-3.5 py-1.5 text-xs text-slate-700 hover:bg-slate-50 flex items-center gap-2 transition-colors">
                                                    <span>Edit</span>
                                                </button>
                                                <button type="button" 
                                                        @onclick="() => OpenFeeModal(student)"
                                                        class="w-full px-3.5 py-1.5 text-xs text-slate-700 hover:bg-slate-50 flex items-center gap-2 transition-colors">
                                                    <span>Collect fee</span>
                                                </button>
                                                <div class="my-1 border-t border-slate-100"></div>
                                                <button type="button" 
                                                        @onclick="() => ToggleStudentStatus(student)"
                                                        class="w-full px-3.5 py-1.5 text-xs text-rose-600 hover:bg-rose-50 flex items-center gap-2 transition-colors">
                                                    <span>@(student.Status == "Active" ? "Deactivate" : "Activate")</span>
                                                </button>
                                            </div>
                                        }
                                    </div>
                                </td>

                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="7" class="py-12 text-center text-slate-400">
                                <div class="max-w-xs mx-auto space-y-2">
                                    <svg class="w-8 h-8 text-slate-300 mx-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"/>
                                    </svg>
                                    <p class="font-bold text-slate-600 text-sm">No students found</p>
                                    <p class="text-xs text-slate-400">Try changing your search query or filter options.</p>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Table Footer / Pagination -->
        <div class="p-4 border-t border-slate-100 flex flex-col sm:flex-row items-center justify-between gap-3 text-xs text-slate-500">
            <div>
                Showing <span class="font-bold text-slate-800">1-@FilteredStudents.Count</span> of <span class="font-bold text-slate-800">@_students.Count</span> students
            </div>
            <div class="flex items-center gap-1.5">
                <button type="button" class="px-3 py-1.5 rounded-lg border border-slate-200 text-slate-400 hover:bg-slate-50 disabled:opacity-40" disabled>
                    Previous
                </button>
                <button type="button" class="w-8 h-8 rounded-lg bg-[#009488] text-white font-bold flex items-center justify-center">
                    1
                </button>
                <button type="button" class="px-3 py-1.5 rounded-lg border border-slate-200 text-slate-700 hover:bg-slate-50">
                    Next
                </button>
            </div>
        </div>
    </div>

</div>

<!-- ========================================================================= -->
<!-- MODAL 1: Add / Edit Student Drawer / Modal -->
<!-- ========================================================================= -->
@if (_showAddEditModal)
{
    <div class="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/60 backdrop-blur-xs p-4">
        <div class="w-full max-w-lg bg-white rounded-3xl shadow-2xl border border-slate-100 p-6 space-y-4 animate-in fade-in zoom-in-95 duration-150">
            <div class="flex items-center justify-between pb-3 border-b border-slate-100">
                <h3 class="text-base font-extrabold text-slate-800">
                    @(_editingStudent == null ? "Add New Student" : "Edit Student Details")
                </h3>
                <button type="button" @onclick="() => _showAddEditModal = false" class="text-slate-400 hover:text-slate-600">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
                </button>
            </div>

            <div class="space-y-3">
                <div class="grid grid-cols-2 gap-3">
                    <div class="space-y-1">
                        <label class="text-[11px] font-semibold text-slate-600">Full Name</label>
                        <input type="text" @bind="_modalModel.Name" placeholder="e.g. Aarav Mehta" class="w-full px-3 py-2 text-xs border border-slate-200 rounded-xl focus:border-[#009488] focus:outline-none" />
                    </div>
                    <div class="space-y-1">
                        <label class="text-[11px] font-semibold text-slate-600">Roll Number</label>
                        <input type="text" @bind="_modalModel.RollNumber" placeholder="e.g. 10A-01" class="w-full px-3 py-2 text-xs border border-slate-200 rounded-xl focus:border-[#009488] focus:outline-none" />
                    </div>
                </div>

                <div class="grid grid-cols-2 gap-3">
                    <div class="space-y-1">
                        <label class="text-[11px] font-semibold text-slate-600">Batch</label>
                        <select @bind="_modalModel.BatchName" class="w-full px-3 py-2 text-xs border border-slate-200 rounded-xl focus:border-[#009488] focus:outline-none">
                            <option value="Class 10 — Maths">Class 10 — Maths</option>
                            <option value="Class 10 — Science">Class 10 — Science</option>
                            <option value="Class 12 — Physics">Class 12 — Physics</option>
                            <option value="Class 12 — Chemistry">Class 12 — Chemistry</option>
                            <option value="IIT-JEE Morning Batch">IIT-JEE Morning Batch</option>
                        </select>
                    </div>
                    <div class="space-y-1">
                        <label class="text-[11px] font-semibold text-slate-600">Enrollment ID</label>
                        <input type="text" @bind="_modalModel.EnrollmentNumber" placeholder="e.g. VM-2026-001" class="w-full px-3 py-2 text-xs border border-slate-200 rounded-xl focus:border-[#009488] focus:outline-none" />
                    </div>
                </div>

                <div class="grid grid-cols-2 gap-3">
                    <div class="space-y-1">
                        <label class="text-[11px] font-semibold text-slate-600">Phone Number</label>
                        <input type="text" @bind="_modalModel.Phone" placeholder="+91 90000 10001" class="w-full px-3 py-2 text-xs border border-slate-200 rounded-xl focus:border-[#009488] focus:outline-none" />
                    </div>
                    <div class="space-y-1">
                        <label class="text-[11px] font-semibold text-slate-600">Parent / Guardian</label>
                        <input type="text" @bind="_modalModel.ParentName" placeholder="e.g. Rohit Mehta" class="w-full px-3 py-2 text-xs border border-slate-200 rounded-xl focus:border-[#009488] focus:outline-none" />
                    </div>
                </div>

                <div class="grid grid-cols-2 gap-3">
                    <div class="space-y-1">
                        <label class="text-[11px] font-semibold text-slate-600">Fee Status</label>
                        <select @bind="_modalModel.FeeStatus" class="w-full px-3 py-2 text-xs border border-slate-200 rounded-xl focus:border-[#009488] focus:outline-none">
                            <option value="Paid">Paid</option>
                            <option value="Partial">Partial</option>
                            <option value="Overdue">Overdue</option>
                        </select>
                    </div>
                    <div class="space-y-1">
                        <label class="text-[11px] font-semibold text-slate-600">Status</label>
                        <select @bind="_modalModel.Status" class="w-full px-3 py-2 text-xs border border-slate-200 rounded-xl focus:border-[#009488] focus:outline-none">
                            <option value="Active">Active</option>
                            <option value="Inactive">Inactive</option>
                        </select>
                    </div>
                </div>
            </div>

            <div class="pt-3 border-t border-slate-100 flex items-center justify-end gap-2">
                <button type="button" @onclick="() => _showAddEditModal = false" class="px-4 py-2 text-xs font-semibold text-slate-600 hover:bg-slate-100 rounded-xl transition-colors">
                    Cancel
                </button>
                <button type="button" @onclick="SaveStudent" class="btn-brand text-xs px-5 py-2">
                    Save Student
                </button>
            </div>
        </div>
    </div>
}

<!-- ========================================================================= -->
<!-- MODAL 2: View Profile Modal -->
<!-- ========================================================================= -->
@if (_selectedProfileStudent != null)
{
    <div class="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/60 backdrop-blur-xs p-4">
        <div class="w-full max-w-md bg-white rounded-3xl shadow-2xl border border-slate-100 p-6 space-y-4 animate-in fade-in zoom-in-95 duration-150">
            <div class="flex items-center justify-between pb-3 border-b border-slate-100">
                <h3 class="text-base font-extrabold text-slate-800">Student Profile</h3>
                <button type="button" @onclick="() => _selectedProfileStudent = null" class="text-slate-400 hover:text-slate-600">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
                </button>
            </div>

            <div class="flex items-center gap-4">
                <div class="w-14 h-14 rounded-2xl bg-teal-50 border border-teal-200 text-[#00796b] font-black text-lg flex items-center justify-center shadow-xs">
                    @_selectedProfileStudent.Initials
                </div>
                <div>
                    <h4 class="text-base font-bold text-slate-900">@_selectedProfileStudent.Name</h4>
                    <p class="text-xs text-slate-400">Roll: @_selectedProfileStudent.RollNumber • ID: @_selectedProfileStudent.EnrollmentNumber</p>
                    <span class="inline-flex items-center px-2 py-0.5 mt-1 rounded-full text-[10px] font-semibold bg-emerald-50 text-emerald-700 border border-emerald-200">
                        @_selectedProfileStudent.Status
                    </span>
                </div>
            </div>

            <div class="bg-slate-50 p-3.5 rounded-2xl space-y-2 text-xs">
                <div class="flex items-center justify-between">
                    <span class="text-slate-400 font-medium">Batch</span>
                    <span class="font-bold text-slate-800">@_selectedProfileStudent.BatchName</span>
                </div>
                <div class="flex items-center justify-between">
                    <span class="text-slate-400 font-medium">Contact</span>
                    <span class="font-bold text-slate-800">@_selectedProfileStudent.Phone</span>
                </div>
                <div class="flex items-center justify-between">
                    <span class="text-slate-400 font-medium">Parent / Guardian</span>
                    <span class="font-bold text-slate-800">@_selectedProfileStudent.ParentName</span>
                </div>
                <div class="flex items-center justify-between">
                    <span class="text-slate-400 font-medium">Fee Status</span>
                    <span class="font-bold @(_selectedProfileStudent.FeeStatus == "Paid" ? "text-emerald-600" : _selectedProfileStudent.FeeStatus == "Partial" ? "text-amber-600" : "text-rose-600")">
                        @_selectedProfileStudent.FeeStatus (₹@_selectedProfileStudent.PaidFee / ₹@_selectedProfileStudent.TotalFee)
                    </span>
                </div>
            </div>

            <div class="pt-2 flex items-center justify-end gap-2">
                <button type="button" @onclick="() => OpenFeeModal(_selectedProfileStudent)" class="btn-brand text-xs px-4 py-2">
                    Collect Fee
                </button>
            </div>
        </div>
    </div>
}

<!-- ========================================================================= -->
<!-- MODAL 3: Collect Fee Modal -->
<!-- ========================================================================= -->
@if (_feeCollectStudent != null)
{
    <div class="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/60 backdrop-blur-xs p-4">
        <div class="w-full max-w-md bg-white rounded-3xl shadow-2xl border border-slate-100 p-6 space-y-4 animate-in fade-in zoom-in-95 duration-150">
            <div class="flex items-center justify-between pb-3 border-b border-slate-100">
                <h3 class="text-base font-extrabold text-slate-800">Collect Student Fee</h3>
                <button type="button" @onclick="() => _feeCollectStudent = null" class="text-slate-400 hover:text-slate-600">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
                </button>
            </div>

            <div class="p-3 bg-teal-50/70 border border-teal-100 rounded-2xl text-xs space-y-1">
                <p class="font-bold text-slate-800">@_feeCollectStudent.Name (@_feeCollectStudent.RollNumber)</p>
                <p class="text-slate-500">Batch: @_feeCollectStudent.BatchName</p>
                <p class="text-slate-500">Total: ₹@_feeCollectStudent.TotalFee | Paid: ₹@_feeCollectStudent.PaidFee | <span class="font-bold text-rose-600">Due: ₹@_feeCollectStudent.DueFee</span></p>
            </div>

            <div class="space-y-3">
                <div class="space-y-1">
                    <label class="text-[11px] font-semibold text-slate-600">Payment Amount (₹)</label>
                    <input type="number" @bind="_feeAmountToCollect" class="w-full px-3 py-2 text-xs border border-slate-200 rounded-xl focus:border-[#009488] focus:outline-none font-bold text-slate-800" />
                </div>
                <div class="space-y-1">
                    <label class="text-[11px] font-semibold text-slate-600">Payment Mode</label>
                    <select class="w-full px-3 py-2 text-xs border border-slate-200 rounded-xl focus:border-[#009488] focus:outline-none">
                        <option>UPI / Online (GPay, PhonePe, Paytm)</option>
                        <option>Cash Receipt</option>
                        <option>Bank Transfer (NEFT/IMPS)</option>
                        <option>Cheque</option>
                    </select>
                </div>
            </div>

            <div class="pt-3 border-t border-slate-100 flex items-center justify-end gap-2">
                <button type="button" @onclick="() => _feeCollectStudent = null" class="px-4 py-2 text-xs font-semibold text-slate-600 hover:bg-slate-100 rounded-xl transition-colors">
                    Cancel
                </button>
                <button type="button" @onclick="ProcessFeePayment" class="btn-brand text-xs px-5 py-2">
                    Confirm & Generate Receipt
                </button>
            </div>
        </div>
    </div>
}

@code {
    private string _searchQuery = string.Empty;
    private string _selectedBatch = string.Empty;
    private string _selectedFeeStatus = string.Empty;
    private string _selectedStatus = string.Empty;
    private string _sortColumn = "Name";
    private bool _isAscending = true;

    private Guid? _activeMenuStudentId;
    private bool _showAddEditModal = false;
    private StudentDto? _editingStudent;
    private StudentDto _modalModel = new();
    private StudentDto? _selectedProfileStudent;
    private StudentDto? _feeCollectStudent;
    private decimal _feeAmountToCollect = 5000;

    private readonly List<StudentDto> _students = new()
    {
        new() { Name = "Aarav Mehta", EnrollmentNumber = "VM-2026-001", RollNumber = "10A-01", BatchName = "Class 10 — Maths", Phone = "+91 90000 10001", ParentName = "Rohit Mehta", Status = "Active", FeeStatus = "Paid", TotalFee = 25000, PaidFee = 25000 },
        new() { Name = "Isha Agarwal", EnrollmentNumber = "VM-2026-002", RollNumber = "10A-02", BatchName = "Class 10 — Maths", Phone = "+91 90000 10002", ParentName = "Neeraj Agarwal", Status = "Active", FeeStatus = "Partial", TotalFee = 25000, PaidFee = 15000 },
        new() { Name = "Kabir Singh", EnrollmentNumber = "VM-2026-014", RollNumber = "10B-04", BatchName = "Class 10 — Science", Phone = "+91 90000 10003", ParentName = "Jaspal Singh", Status = "Inactive", FeeStatus = "Overdue", TotalFee = 28000, PaidFee = 0 },
        new() { Name = "Ananya Rao", EnrollmentNumber = "VM-2026-021", RollNumber = "10B-07", BatchName = "Class 10 — Science", Phone = "+91 90000 10004", ParentName = "Suresh Rao", Status = "Active", FeeStatus = "Paid", TotalFee = 28000, PaidFee = 28000 },
        new() { Name = "Devansh Jain", EnrollmentNumber = "VM-2026-044", RollNumber = "12P-03", BatchName = "Class 12 — Physics", Phone = "+91 90000 10005", ParentName = "Mahesh Jain", Status = "Active", FeeStatus = "Partial", TotalFee = 32000, PaidFee = 20000 },
        new() { Name = "Ridhima Kaur", EnrollmentNumber = "VM-2026-052", RollNumber = "12C-05", BatchName = "Class 12 — Chemistry", Phone = "+91 90000 10006", ParentName = "Harleen Kaur", Status = "Active", FeeStatus = "Paid", TotalFee = 32000, PaidFee = 32000 },
        new() { Name = "Vivaan Chopra", EnrollmentNumber = "VM-2026-070", RollNumber = "JEE-11", BatchName = "IIT-JEE Morning Batch", Phone = "+91 90000 10007", ParentName = "Amit Chopra", Status = "Active", FeeStatus = "Overdue", TotalFee = 65000, PaidFee = 20000 },
        new() { Name = "Meera Iyer", EnrollmentNumber = "VM-2026-071", RollNumber = "JEE-12", BatchName = "IIT-JEE Morning Batch", Phone = "+91 90000 10008", ParentName = "Ganesh Iyer", Status = "Active", FeeStatus = "Paid", TotalFee = 65000, PaidFee = 65000 }
    };

    private List<StudentDto> FilteredStudents
    {
        get
        {
            var query = _students.Where(s =>
                (string.IsNullOrWhiteSpace(_searchQuery) || 
                 s.Name.Contains(_searchQuery, StringComparison.OrdinalIgnoreCase) || 
                 s.RollNumber.Contains(_searchQuery, StringComparison.OrdinalIgnoreCase) || 
                 s.EnrollmentNumber.Contains(_searchQuery, StringComparison.OrdinalIgnoreCase) ||
                 s.Phone.Contains(_searchQuery, StringComparison.OrdinalIgnoreCase)) &&
                (string.IsNullOrWhiteSpace(_selectedBatch) || s.BatchName == _selectedBatch) &&
                (string.IsNullOrWhiteSpace(_selectedFeeStatus) || s.FeeStatus == _selectedFeeStatus) &&
                (string.IsNullOrWhiteSpace(_selectedStatus) || s.Status == _selectedStatus)
            );

            query = _sortColumn switch
            {
                "Name" => _isAscending ? query.OrderBy(s => s.Name) : query.OrderByDescending(s => s.Name),
                "Roll" => _isAscending ? query.OrderBy(s => s.RollNumber) : query.OrderByDescending(s => s.RollNumber),
                "Batch" => _isAscending ? query.OrderBy(s => s.BatchName) : query.OrderByDescending(s => s.BatchName),
                "Contact" => _isAscending ? query.OrderBy(s => s.Phone) : query.OrderByDescending(s => s.Phone),
                "Status" => _isAscending ? query.OrderBy(s => s.Status) : query.OrderByDescending(s => s.Status),
                "Fees" => _isAscending ? query.OrderBy(s => s.FeeStatus) : query.OrderByDescending(s => s.FeeStatus),
                _ => query.OrderBy(s => s.Name)
            };

            return query.ToList();
        }
    }

    private void SortBy(string column)
    {
        if (_sortColumn == column)
        {
            _isAscending = !_isAscending;
        }
        else
        {
            _sortColumn = column;
            _isAscending = true;
        }
    }

    private void ToggleMenu(Guid studentId)
    {
        _activeMenuStudentId = _activeMenuStudentId == studentId ? null : studentId;
    }

    private void OpenAddModal()
    {
        _editingStudent = null;
        _modalModel = new StudentDto
        {
            EnrollmentNumber = $"VM-2026-{(_students.Count + 1):D3}",
            BatchName = "Class 10 — Maths",
            FeeStatus = "Paid",
            Status = "Active"
        };
        _showAddEditModal = true;
    }

    private void OpenEditModal(StudentDto student)
    {
        _activeMenuStudentId = null;
        _editingStudent = student;
        _modalModel = new StudentDto
        {
            Id = student.Id,
            Name = student.Name,
            EnrollmentNumber = student.EnrollmentNumber,
            RollNumber = student.RollNumber,
            BatchName = student.BatchName,
            Phone = student.Phone,
            ParentName = student.ParentName,
            Status = student.Status,
            FeeStatus = student.FeeStatus,
            TotalFee = student.TotalFee,
            PaidFee = student.PaidFee
        };
        _showAddEditModal = true;
    }

    private void SaveStudent()
    {
        if (string.IsNullOrWhiteSpace(_modalModel.Name)) return;

        if (_editingStudent != null)
        {
            var idx = _students.FindIndex(s => s.Id == _editingStudent.Id);
            if (idx >= 0)
            {
                _students[idx] = _modalModel;
            }
        }
        else
        {
            _students.Insert(0, _modalModel);
        }

        _showAddEditModal = false;
        _editingStudent = null;
    }

    private void OpenViewProfile(StudentDto student)
    {
        _activeMenuStudentId = null;
        _selectedProfileStudent = student;
    }

    private void OpenFeeModal(StudentDto student)
    {
        _activeMenuStudentId = null;
        _selectedProfileStudent = null;
        _feeCollectStudent = student;
        _feeAmountToCollect = student.DueFee > 0 ? student.DueFee : 5000;
    }

    private void ProcessFeePayment()
    {
        if (_feeCollectStudent != null)
        {
            _feeCollectStudent.PaidFee += _feeAmountToCollect;
            if (_feeCollectStudent.PaidFee >= _feeCollectStudent.TotalFee)
            {
                _feeCollectStudent.FeeStatus = "Paid";
            }
            else
            {
                _feeCollectStudent.FeeStatus = "Partial";
            }
        }
        _feeCollectStudent = null;
    }

    private void ToggleStudentStatus(StudentDto student)
    {
        student.Status = student.Status == "Active" ? "Inactive" : "Active";
        _activeMenuStudentId = null;
    }

    private void ExportCsv()
    {
        // CSV export action
    }
}
